Dependencies:
- concurrent.futures
- mock
- enum34

This code doesn't do all the Lamello stuff, but just enough to run the Philips
Hue demo for the thesis. You can access it with "hue.py" (which opens a useless
user interface).
